@model List<Slider>
@{
    ViewData["Title"] = "Index";
    int order = 0;
}

<div class="content-wrapper">
    <div class="row">
        <a class="btn btn-primary btn-lg" asp-action="CreateMultiple">Add Multiple Images</a>
        <div class="col-lg-12 grid-margin stretch-card">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title">Basic Table</h4>
                    <p class="card-description">
                        Add class <code>.table</code>
                    </p>
                    <div class="table-responsive">
                        <table class="table">
                            <thead>
                                <tr>
                                    <th>Order</th>
                                    <th>Title</th>
                                    <th>Desc</th>
                                    <th>Sign</th>
                                    <th>Operations</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    order++;
                                    <tr>
                                        <td>@order</td>
                                        <td>@Html.Raw(item.Title)</td>
                                        <td>@item.Desc</td>
                                        <td><img src="~/img/@item.Sign" style="width:200px;" /></td>
                                        <td>
                                            <a class="btn btn-primary" asp-controller="Slider" asp-action="Detail" asp-route-id="@item.Id">Detail</a>
                                            <a class="btn btn-warning" asp-controller="Slider" asp-action="Update" asp-route-id="@item.Id">Update</a>
                                            <a class="btn btn-danger" asp-controller="Slider" asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

