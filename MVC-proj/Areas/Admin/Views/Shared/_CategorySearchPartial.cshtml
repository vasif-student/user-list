@model List<Category>

@if (Model.Count == 0)
{
    <li>Netice tapilmadi</li>
}
else
{
    @foreach (var item in Model)
    {
        <li><i>Category Name: </i><b>@item.Name</b>  |  <i>Category Desc: </i><b>@Html.Raw(item.Description)</b></li>
    }
}